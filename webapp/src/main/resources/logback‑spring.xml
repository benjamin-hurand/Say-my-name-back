<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Define a property for the log file location -->
  <property name="LOG_FILE" value="logs/app.log" />

  <!-- Console Appender -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- File Appender -->
  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>${LOG_FILE}</file>
    <append>true</append>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Reduce Hibernate SQL logging -->
  <logger name="org.hibernate.SQL" level="ERROR" additivity="false">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="FILE" />
  </logger>

  <!-- Reduce Hibernate parameter binding logs -->
  <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="ERROR" additivity="false">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="FILE" />
  </logger>

  <!-- Optionally reduce other noisy Hibernate loggers -->
  <logger name="org.hibernate.tool.hbm2ddl" level="ERROR" additivity="false">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="FILE" />
  </logger>

  <!-- Root logger configuration -->
  <root level="DEBUG">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="FILE" />
  </root>
</configuration>
